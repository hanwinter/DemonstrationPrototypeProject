请基于当前家长端移动端“体检报名”页面继续优化。不要重做整个项目，不要改变体检报名、自费项目选择、支付、退款等业务逻辑，只调整页面层级、底部 Tab 显示和底部结算栏位置。

一、体检报名页面定位调整

将“体检报名”页面定位为首页快捷入口进入的二级业务页面，而不是底部 Tab 一级页面。

进入路径：

首页 → 快捷入口 → 体检报名

要求：
1. 首页快捷入口中继续保留“体检报名”。
2. 点击首页快捷入口“体检报名”后进入体检报名页面。
3. 体检报名页面作为二级业务页面展示。
4. 页面顶部保留返回按钮。
5. 点击返回按钮返回首页。
6. 不要把体检报名作为底部 Tab 一级频道。

二、隐藏体检报名页面底部 Tab

当前体检报名页面底部如果显示：首页、问卷、报告、我的 等 Tab，请在体检报名页面隐藏。

要求：
1. 体检报名页面不显示底部 Tab。
2. 页面底部不要出现：首页、问卷、报告、我的。
3. 不影响首页和其他一级页面的底部 Tab。
4. 首页仍然可以显示底部 Tab。
5. 问卷、报告、我的等一级页面仍可保留底部 Tab。
6. 只在体检报名这个二级页面隐藏底部 Tab。

三、修正底部结算栏位置

当前体检报名页面的底部结算栏位置过高，悬浮在页面内容中间，遮挡“基础筛查项目”“学生信息确认”等内容。

请修正为：

1. 底部结算栏固定在当前移动端页面底部。
2. 因为体检报名页隐藏底部 Tab，所以结算栏应贴近屏幕底部安全区。
3. 结算栏不要悬浮在内容中间。
4. 结算栏宽度与手机页面容器一致，不要超出容器。
5. 页面滚动时，结算栏保持固定。
6. 页面内容滚动到最底部时，不被结算栏遮挡。

建议布局：

页面内容
--------------------------
已选 0 项 / ￥0    确认基础报名
--------------------------

四、页面内容底部留白

隐藏底部 Tab 后，页面主体内容需要重新计算底部留白。

要求：
1. 内容滚动区域增加 padding-bottom。
2. padding-bottom 至少覆盖底部结算栏高度 + 安全距离。
3. 建议 padding-bottom 不低于 100px。
4. 页面最后一个内容区，如“备注”“自费订单”“自费项目列表最后一项”，不能被结算栏遮挡。
5. 不要出现结算栏盖住正文内容的情况。

五、底部结算栏样式保持当前风格

结算栏视觉风格可以保持当前样式，但位置要正确。

要求：
1. 保持当前浅色背景、圆角、阴影、主按钮样式。
2. 左侧显示已选数量和金额。
3. 右侧显示当前操作按钮。
4. 未报名状态：按钮显示“确认基础报名”。
5. 选择自费项目后：按钮显示“提交自费订单并支付”或当前已有文案。
6. 已支付状态：按钮显示退款相关操作。
7. 不改变已有状态切换逻辑。
8. 不改变支付成功、退款成功、报名成功弹框逻辑。

六、页面层级与其他页面保持一致

体检报名页面应与复筛通知页面保持一致的二级页面逻辑：

1. 都从首页快捷入口进入。
2. 都有顶部返回按钮。
3. 都不显示底部 Tab。
4. 都专注当前业务任务。
5. 首页负责承载入口，二级页面负责承载具体业务流程。

七、业务逻辑保持不变

本次优化不能改变体检报名页面已有业务逻辑：

1. 免费基础报名是主流程。
2. 自费项目是基础报名后的可选流程。
3. 自费项目绑定本次体检计划。
4. 自费订单独立支付。
5. 退款只影响自费订单，不影响基础报名。
6. 退款成功后，在截止时间前可以重新选择自费项目。
7. 基础报名成功、付款成功、退款成功的弹框逻辑保持不变。
8. 已支付状态下自费项目不可编辑。
9. 退款成功后自费项目恢复可选。

八、验收标准

完成后页面应满足：

1. 体检报名页不显示底部 Tab。
2. 首页仍然显示底部 Tab。
3. 体检报名页底部结算栏固定在页面底部，不悬浮在内容中间。
4. 底部结算栏不遮挡基础筛查项目、学生信息确认、自费项目和备注内容。
5. 页面滚动到底部时，最后一项内容完整可见。
6. 顶部返回按钮可以返回首页。
7. 原有报名、支付、退款逻辑不受影响。