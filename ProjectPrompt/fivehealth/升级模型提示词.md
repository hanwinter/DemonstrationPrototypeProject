你现在在一个已经完成的 Vue3 + Vite 项目中，这个项目已经存在“儿童青少年五健筛查系统”的初筛页面功能（包括：列表、筛查流程、数据展示、交互逻辑等）。

⚠️ 重要约束（必须严格遵守）：
1. 不允许修改任何已有页面结构、样式或业务逻辑
2. 不允许重构现有代码
3. 不允许删除任何已有组件或功能
4. 只能“新增模块 + 最小侵入式集成”
5. 新功能必须完全可插拔（可删除不影响原系统）

---

## 🎯 本次任务目标

在系统中新增一个独立模块：

👉 “五健个体健康画像 - 3D人体可视化模块”
功能要求如下：

---

## 1️⃣ 技术实现

使用 Three.js 实现一个3D人体模型展示模块：

- 使用现成 GLTF/GLB 人体模型（不需要你建模）
- 模型路径：/public/models/human.glb
- 支持 WebGL 渲染
- 使用 OrbitControls 支持旋转/缩放/拖拽

---

## 2️⃣ 模块集成方式（关键）

必须采用“独立组件方式”，不能嵌入原有业务逻辑：

新增组件：

📁 src/components/three/ThreeHumanViewer.vue

该组件要求：
- 完全独立
- 只接收 props 数据
- 不依赖原业务代码
- 可在任何页面插入

---

## 3️⃣ 异常数据驱动高亮（核心）

组件接收如下数据：

```ts
props: {
  abnormalities: [
    {
      organ: "eye_left",
      severity: "high" | "moderate" | "low",
      name: string
    }
  ]
}
4️⃣ 器官映射规则（必须实现）

在 three.js 模型中，根据 mesh.name 进行匹配：

organ	mesh.name
eye_left	eye_left
eye_right	eye_right
spine	spine
mouth	mouth
body	body
5️⃣ 高亮规则（必须实现）

不同严重程度使用不同发光效果：

low → 黄色 glow
moderate → 橙色 glow
high → 红色强发光

实现方式：

修改 material.emissive
或增加 emissiveIntensity
或 outline effect（可选）
6️⃣ 交互要求

必须支持：

点击器官（raycaster）
高亮当前选中器官
console 输出当前 organ + name
7️⃣ 页面集成方式（必须做到最小侵入）

在现有“初筛页面”中，仅新增一个区域：

👉 3D人体展示区域（右侧或弹窗均可）

示例：

<ThreeHumanViewer :abnormalities="abnormalList" />

⚠️ 不允许改动原有初筛逻辑，只允许新增 import + 组件引用

8️⃣ 性能要求
模型加载需 lazy load
renderer 必须 dispose 防止内存泄漏
支持组件销毁清理 three.js scene
9️⃣ 代码结构要求

新增目录：

src/components/three/

ThreeHumanViewer.vue
useThreeHuman.ts（可选 composable）
modelLoader.ts（可选）
🔟 额外要求（重要）

必须保证：

原系统完全不受影响
3D模块失败不会影响页面加载
支持开关控制（show3D = true/false）
🚀 最终交付

请输出完整可运行代码：

ThreeHumanViewer.vue
three.js 初始化逻辑
模型加载逻辑
高亮逻辑
点击交互逻辑
与现有Vue页面集成示例