请基于当前家长端移动端“复筛通知”页面继续优化。不要重做整个项目，不要改变首页快捷入口逻辑，不要恢复底部 Tab；复筛通知仍然作为首页快捷入口进入的二级页面。只针对当前复筛通知页面的信息层级、状态展示和操作按钮做减法优化。

一、删除顶部提醒区域

当前页面标题下方有一张提醒卡片，内容类似：

“您有 1 条待处理复筛通知”
“请确认复筛安排，按通知时间到指定科室完成复查。”

请删除整个提醒区域。

要求：
1. 删除该提醒卡片。
2. 删除后页面直接展示复筛通知主体卡片。
3. 不要保留“您有 1 条待处理复筛通知”这类重复提示。
4. 删除后适当调整页面间距，避免顶部区域显得空。

二、简化状态展示

当前页面底部或中部存在多个状态按钮或状态标签，例如：

- 已通知
- 已确认
- 待复筛
- 已完成

请删除“已通知”和“已确认”这两个状态。

复筛通知页面只保留与家长最相关的状态：

1. 待复筛
2. 已完成

要求：
1. 不要再展示“已通知”。
2. 不要再展示“已确认”。
3. 不要做一排流程状态按钮。
4. 当前通知卡片只显示一个当前状态。
5. 状态仅作为展示，不需要可点击。
6. 状态建议放在复筛通知卡片左上角或标题附近。

当前页面如果是未完成复筛，状态显示为：

“待复筛”

如果需要演示已完成状态，可以在模拟数据中保留一条“已完成”数据，但当前主卡片默认显示“待复筛”。

三、删除底部操作按钮

当前页面底部有两个按钮：

- 查看详情
- 已确认复筛

请删除这两个按钮。

原因：
1. 页面中已经直接展示“复筛详情”，不需要“查看详情”按钮。
2. “已确认复筛”容易让家长误解为已经完成复筛。
3. 该页面定位为复筛通知详情页，不需要复杂操作。

要求：
1. 删除“查看详情”按钮。
2. 删除“已确认复筛”按钮。
3. 删除按钮区域后，页面底部不留大块空白。
4. 页面可以自然滚动结束。

四、保留复筛通知主体内容

复筛通知主体卡片保留，但结构可以更清爽。

建议保留字段：

- 学生姓名：林一凡
- 页面标题或卡片标题：复筛通知
- 状态：待复筛
- 通知来源：第一实验小学 / 妇幼保健院
- 复筛项目：视力、脊柱
- 复筛时间：2026-04-18 09:00
- 复筛地点：儿童保健科
- 联系电话：010-66886688

要求：
1. 状态只显示一个“待复筛”。
2. 不要再显示“已确认”状态。
3. 字段信息保持清晰对齐。
4. 移动端不要出现字段折行过多的问题。

五、保留复筛详情内容

复筛详情卡片保留。

内容可以继续展示：

异常原因：
左眼裸眼视力下降，筛查提示脊柱姿态需进一步复核。

注意事项：
请携带学生证件，提前 10 分钟到达；如近期已完成眼科或骨科检查，可携带相关资料。

要求：
1. “复筛详情”作为独立卡片展示。
2. 不需要再通过“查看详情”按钮展开。
3. 详情内容直接展示在页面中。
4. 文本排版清晰，行距适中。

六、页面定位保持不变

复筛通知仍然是首页快捷入口进入的二级页面。

要求：
1. 页面顶部保留返回按钮。
2. 页面标题保留“复筛通知”。
3. 点击返回按钮返回首页。
4. 页面不显示底部 Tab。
5. 不要新增底部导航。
6. 首页快捷入口“复筛通知”保持不变。

七、视觉风格要求

继续保持当前儿童健康管理平台移动端风格：

- 浅绿色、浅蓝绿色、白色卡片风格
- 页面清爽，不要过度装饰
- 信息层级清楚
- 不要堆叠太多状态
- 不要引入复杂图标
- 不要新增无关功能
- 页面整体更像“复筛通知详情页”，而不是“复筛流程管理页”

八、验收标准

完成后页面应满足：

1. 看不到“您有 1 条待处理复筛通知”提醒卡片。
2. 看不到“已通知”状态。
3. 看不到“已确认”状态。
4. 看不到“查看详情”按钮。
5. 看不到“已确认复筛”按钮。
6. 页面只显示当前状态“待复筛”或“已完成”。
7. 页面无底部 Tab。
8. 复筛通知信息和复筛详情直接展示。