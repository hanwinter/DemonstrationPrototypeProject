继续优化五健专项详情底部弹框的内容展示。

当前问题：
1. 当前结论是纯文本，异常指标和健康建议是背景块，三种样式混用，导致内容视觉上始终无法完全左对齐。
2. 异常指标和健康建议背景块带有内边距，正文比当前结论正文更靠右。
3. 背景块让异常指标和健康建议看起来像当前结论的二级内容，信息层级不准确。

本次调整目标：
放弃继续微调背景块 padding，统一改成“标题 + 纯文本正文”的轻量展示方式。

修改范围：
只修改底部弹框中“当前结论、异常指标、健康建议”的展示方式，不修改五健切换页签、不修改标题区、不修改检查信息表格、不修改主页面。

具体要求：

1. 当前结论、异常指标、健康建议三个模块全部使用相同结构：
   - block-title
   - block-content

2. 删除异常指标内容块的白色背景。
3. 删除健康建议内容块的浅青色背景。
4. 删除异常指标和健康建议内容块的 border、border-radius、padding 背景卡片样式。
5. 三个模块的标题左对齐。
6. 三个模块的正文左对齐。
7. 三个模块都从同一个内容区左边界开始。
8. 健康建议正文使用青绿色加粗文字突出，不使用背景块。
9. 检查信息表格保持原样。

推荐结构：

<section class="detail-block">
  <div class="block-title">当前结论</div>
  <div class="block-content strong-content">
    {{ activeHealth.description }}
  </div>
</section>

<section class="detail-block">
  <div class="block-title">异常指标</div>
  <div class="block-content strong-content">
    {{ activeHealth.abnormalValues.join('、') }}
  </div>
</section>

<section class="detail-block">
  <div class="block-title">健康建议</div>
  <div class="block-content advice-text">
    {{ activeHealth.advice }}
  </div>
</section>

CSS 建议：

.sheet-scroll-body {
  padding: 20px 24px 28px;
  box-sizing: border-box;
}

.detail-block {
  width: 100%;
  box-sizing: border-box;
  margin-bottom: 20px;
}

.block-title {
  margin-bottom: 8px;
  font-size: 14px;
  font-weight: 600;
  color: #8ca1a8;
}

.block-content {
  width: 100%;
  box-sizing: border-box;
  font-size: 15px;
  line-height: 1.65;
  color: #17313a;
}

.strong-content {
  font-weight: 700;
}

.advice-text {
  color: #00a9a5;
  font-weight: 700;
  line-height: 1.65;
}

请删除或覆盖以下样式：
1. .abnormal-line 的背景、边框、圆角、内边距；
2. .soft-content 的背景、边框、圆角、内边距；
3. .advice-content 的背景、边框、圆角、内边距；
4. .advice-block 内导致内容块像卡片的样式；
5. 任何 margin-left、margin-right、width: 100vw、calc(100% + xx) 等横向扩展样式。

最终视觉效果：
当前结论、异常指标、健康建议三个模块应全部左对齐，阅读连续，不再出现背景块错位问题。