请基于当前家长端首页做 UI 优化，只调整页面布局、间距、字号、颜色层级和组件样式，不改变现有业务功能、跳转逻辑和数据结构。

优化目标：
让页面更清爽、更有主次，减少首屏拥挤感，突出“体检报告”和“健康知识推荐”两个核心内容。

具体调整要求：

1. 顶部区域
- 保留当前顶部返回按钮、标题、Banner 图和学生信息卡。
- 适当增加 Banner 与学生信息卡之间的呼吸感，避免信息卡紧贴 Banner。
- 学生信息卡整体高度略压缩。
- “林一凡｜五年级2班”字号略调小，但仍保持为主信息。
- “当前就诊人”弱化为浅灰小字。
- “切换”按钮改为更轻量的小胶囊按钮，尺寸略小，颜色保持清爽。

2. 快捷入口区域
- 四个快捷入口顺序保持：体检报名、问卷量表、体检报告、复筛通知。
- 统一四个 icon 的视觉风格：尺寸一致、底色容器一致、阴影和圆角一致。
- icon 可适当放大，但入口区域整体高度要压缩。
- 快捷入口文字字号略小，字重降低，避免抢视觉重点。
- 保持四等分布局，间距均匀。

3. 体检报告卡片
- 保留“林一凡 的体检报告”“2026年6月9日 体检”“存在 3 项需关注指标”和异常标签。
- 标题字号略降，但仍保持加粗。
- “存在 3 项需关注指标”作为重点信息展示，可以适当提高可读性，但不要使用过重背景。
- 异常标签颜色降低饱和度，保持柔和，不要红橙蓝同时过于抢眼。
- “查看详情”弱化为右上角轻量文字 + 箭头，或改为小型入口，不要像主按钮一样突出。
- 卡片整体内边距适当压缩，减少纵向空间占用。

4. 健康知识区域
- “健康知识”标题保留，字号和字重适当降低。
- 分类标签保持横向滚动。
- 分类标签高度略压缩，选中态颜色更浅。
- 确保“口腔保健”等标签不被明显截断；如横向滚动，右侧增加轻微渐隐效果，提示可继续滑动。
- 标签之间间距均匀，不要显得拥挤。

5. 宣教文章列表
- 保留当前文章卡片结构：左图、右侧标题、分类/阅读量/已读状态、推荐标签。
- 适当缩小文章图片尺寸，统一圆角。
- 卡片高度压缩，让首屏能看到更多内容。
- 文章标题最多两行，超出省略。
- 分类、阅读量、已读状态使用浅灰小字，降低视觉权重。
- “根据报告推荐”标签弱化展示，保持小胶囊样式，不要过于抢眼。
- 卡片阴影和边框保持轻量，避免页面变重。

6. 底部 Tab 栏
- 保持四个 Tab：首页、问卷、报告、我的。
- 四个 Tab 平均分布在底部。
- 首页选中态背景缩小，只包住图标和文字，不要像大按钮。
- 未选中图标和文字颜色更浅，选中态保持当前主色。
- 底部栏固定在页面底部，避免遮挡内容，列表底部需要预留安全距离。

7. 整体视觉规范
- 页面主色继续使用清新的青绿色系。
- 减少过重的蓝色、红色、橙色视觉块。
- 页面圆角、阴影、边框保持统一。
- 优先通过字号、字重、留白、颜色深浅建立层级。
- 不新增复杂动效，不新增无关模块。
- 适配移动端宽度，避免文字挤压、按钮溢出、卡片遮挡和横向滚动异常。

请完成后重点检查：
- 首屏是否比之前更清爽；
- 体检报告卡片是否成为主要视觉重点；
- 快捷入口和健康知识是否不再抢主内容；
- 底部 Tab 是否平均且不遮挡列表内容；
- 横向标签滚动是否自然。