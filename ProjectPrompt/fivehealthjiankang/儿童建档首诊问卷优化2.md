请继续优化当前家长端 H5 第二版“五健智行/专案管理”中的“儿童建档”二级页面，重点修复编辑态 UI 细节。

一、修复必填项红点位置

当前问题：
在编辑状态下，必填项红色星号/红点显示在输入框下方，位置不对，导致页面视觉凌乱。

调整要求：
1. 必填标识必须放在字段标题旁边，而不是输入框下方。
2. 推荐样式：
   - 字段标题：儿童姓名 *
   - 红色星号紧跟字段标题右侧；
   - 星号字号小一些，颜色使用柔和红色；
   - 不要单独占一行；
   - 不要出现在输入框底部。
3. 所有必填字段统一处理，包括：
   - 儿童姓名
   - 性别
   - 出生日期
   - 身份证号
   - 就读机构
   - 年级
   - 班级
   - 监护人姓名
   - 与儿童关系
   - 联系电话
   - 紧急联系人
   - 紧急联系人电话
   - 户籍地址
   - 现居住地址
   - 民族
   - 出生孕周
   - 是否早产
   - 分娩方式
4. 查看态不需要显示红色星号；只有编辑态显示必填标识。
5. 不要因为星号导致字段标题换行或错位。

二、调整学校 mock 数据，测试长文本折行

当前问题：
学校名称较短，看不出长文本在移动端下的展示效果。

调整要求：
1. 将“就读机构/学校”的 mock 数据改成长一些，例如：
   - 北京市海淀区中关村第三小学万柳校区
   或：
   - 北京市海淀区某某实验学校小学部
2. 在查看态下：
   - 长学校名称允许自然换行；
   - 不要横向滚动；
   - 不要溢出卡片；
   - 字段高度可自适应；
   - 文本行高保持舒适。
3. 在编辑态下：
   - 输入框也要支持长文本显示；
   - 如果是单行输入框，可允许内容横向滚动但不能撑破布局；
   - 更推荐对“就读机构/学校”使用可换行的输入框样式或自适应高度展示样式；
   - 保证 390px 移动端宽度下不挤压相邻字段。
4. 如果“就读机构”和“年级”当前在同一行两列布局中，长学校名称会挤压布局时，请将“就读机构/学校”单独占一整行，年级和班级仍可保持两列。

三、删除提示文案

请删除页面中这句提示文案：

“以下为长期健康风险信息，不录入专项筛查结果。”

删除后不要留下空白区域，相关卡片间距自然衔接。

四、验收重点

请重点检查：
1. 编辑态所有必填星号是否都在字段标题右侧；
2. 是否还存在输入框下方孤立的红色星号；
3. 学校名称变长后，查看态是否正常折行；
4. 学校名称变长后，编辑态是否不撑破两列布局；
5. 删除提示文案后页面是否没有异常空白；
6. 390px 移动端宽度下是否无横向滚动、无内容重叠。