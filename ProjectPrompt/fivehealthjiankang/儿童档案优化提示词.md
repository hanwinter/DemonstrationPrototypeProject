修改“五健健康管理家长端 H5-专案/儿童建档”页面，参考当前截图中的页面样式进行调整。

一、修改范围

只修改：
“五健健康管理家长端 H5 / 专案 / 儿童建档”二级页面。

不要影响：
首页、专案首页、首诊问卷、知情同意书、报告、康复、底部 Tab、其他二级页面。

二、当前页面保留内容

保留当前页面已有结构和样式：

1. 顶部手机页面外框尺寸
   - 保持当前移动端宽度比例，不要拉宽页面。
   - 页面仍然是手机 H5 预览尺寸。
   - 不要因为字段增多导致页面变成 PC 宽屏布局。

2. 顶部栏
   - 保留“儿童健康管理平台”顶部栏。
   - 保留返回按钮和右侧图标。
   - 保持当前浅青色背景风格。

3. 二级页面标题
   - 保留“儿童建档”标题。
   - 保留标题左侧返回箭头。
   - 样式与当前截图一致。

4. 孩子信息卡片
   - 保留顶部孩子信息卡片：
     “林一凡｜男｜10岁”
     “视力健康管理专案”
     状态标签“已保存未提交”
   - 状态标签继续放在右侧，样式保持轻量。

5. 基础身份信息
   - 保留当前“基础身份信息”分组。
   - 保留字段：
     儿童姓名、性别、出生日期、年龄、身份证号、档案编号。
   - 保持当前两列布局。

6. 就读信息
   - 保留当前“就读信息”分组。
   - 保留字段：
     就读机构、年级、班级、学籍号。
   - 就读机构字段较长时允许换行或完整显示，不要横向溢出。

三、删除内容

删除“就读信息”下面现有的全部旧内容。

包括但不限于：
- 监护人信息
- 紧急联系人
- 生活行为基线
- 长期健康风险说明
- 其他旧建档字段

也就是说：
页面从“就读信息”之后，全部改为 Excel《儿保建档字段.xlsx》中的建档字段分组。

四、新增字段分组

在“就读信息”下面新增以下分组，字段来自 Excel《儿保建档字段.xlsx》。

新增分组一：手册信息

字段包括：
胎数、出生次序、出生地点、受孕方式、出生情况、出生身长（cm）、出生体重（kg）、出生头围（cm）、孕周（X周X天）、第几胎、第几产、助产机构、阿氏评分、阿氏评分-1分钟、阿氏评分-5分钟、阿氏评分-10分钟、新生儿听力筛查、新生儿听力筛查-未通过、新生儿疾病筛查、新生儿疾病筛查-其他遗传代谢病、建档人、建档机构。

新增分组二：母亲信息

字段包括：
母亲姓名、联系电话、民族、出生日期、母亲当前情况、证件类型、证件号码、文化程度、母亲职业、工作单位、健康状况、健康状况-慢性病、健康状况-传染病、母亲妊娠患病情况、母亲妊娠患病情况-其他、孕期是否有传染病、艾梅乙阳性、孕期高危等级、孕期高危因素。

新增分组三：父亲信息

字段包括：
父亲姓名、联系电话、民族、出生日期、证件类型、证件号码、父亲职业、工作单位、文化程度、健康状况、健康状况-慢性病、健康状况-传染病。

注意：
父亲信息中的“工作单位”按文本输入处理，不按多选处理。

新增分组四：高危信息

字段包括：
是否高危、高危因素、其他高危因素、住院诊断。

五、字段展示方式

参考当前截图中的表单样式：

1. 分组卡片
   - 使用当前白色卡片样式。
   - 圆角、阴影、间距与截图保持一致。
   - 不要做成很厚重的大卡片。
   - 字段较多的分组内部要紧凑排版。

2. 字段布局
   - 短字段优先使用两列布局。
   - 长字段使用单列布局。
   - 多选、长文本、住院诊断等使用单列。
   - 字段值不能溢出卡片。
   - 长内容允许换行。

3. 只读与编辑状态
   - 未点击“编辑资料”时，字段保持只读展示。
   - 点击“编辑资料”后，字段变为可编辑。
   - 可编辑字段仍然保持当前浅灰输入框风格。
   - 不要使用弹窗编辑。

六、字段类型与默认值

按 Excel 字段规则处理：

- 单选字段使用选择器或单选项。
- 多选字段使用多选标签或多选选择器。
- 日期字段格式为 yyyy-MM-dd。
- 文本域用于“住院诊断”等长文本。
- 档案编号、建档人、建档机构为只读展示。
- 是否高危默认“否”。
- 户籍类型默认“户籍”。
- 户别默认“非农业”。
- 国籍默认“中国”。
- 民族默认“汉族”。
- 胎数默认“单胎”。
- 出生次序默认“1”。
- 出生地点默认“县（区）级医院”。
- 受孕方式默认“自然受孕”。
- 出生情况默认“顺产”。
- 出生身长默认“50”。
- 出生体重默认“2.5”。
- 母亲当前情况默认“正常”。
- 母亲证件类型默认“居民身份证”。

七、底部按钮优化

当前截图中底部“编辑资料 / 提交”按钮是固定在底部的，这个设计可以保留，但需要优化：

1. 底部按钮固定在手机页面底部。
2. 按钮区域背景要有轻微遮罩或白色底，避免压住正文时看不清。
3. 页面正文底部需要增加 padding-bottom，确保最后一组字段不会被按钮遮挡。
4. 按钮样式保持当前风格：
   - 左侧“编辑资料”为浅色按钮。
   - 右侧“提交”为主色按钮。
5. 提交后状态改为“已提交”，按钮可根据现有逻辑变为“返回”或禁用提交，保持项目已有交互规则。

八、交互规则

保留当前儿童建档页已有逻辑：

1. 点击“编辑资料”后进入编辑状态。
2. 点击“保存”后状态为“已保存未提交”。
3. 点击“提交”后状态为“已提交”。
4. 不新增弹窗。
5. 不改变返回逻辑。
6. 不影响页面滚动。
7. 字段很多时页面正常纵向滚动。

九、移动端适配要求

重点检查截图中的手机尺寸：

1. 页面不能变宽。
2. 手机外框不能变形。
3. 内容不能出现横向滚动。
4. 字段不能超出卡片。
5. 底部按钮不能遮挡最后一组字段。
6. 滚动条位置正常。
7. 在 375px 左右宽度下展示正常。

十、验收标准

完成后需要满足：

1. 页面仍然保持截图中的移动端 H5 风格。
2. 顶部孩子信息、基础身份信息、就读信息全部保留。
3. 就读信息下面旧内容全部删除。
4. 新增“手册信息、母亲信息、父亲信息、高危信息”四组字段。
5. 字段内容与 Excel《儿保建档字段.xlsx》一致。
6. 父亲“工作单位”为文本输入。
7. 底部按钮不遮挡内容。
8. 手机页面尺寸不变形。
9. 编辑、保存、提交状态正常。