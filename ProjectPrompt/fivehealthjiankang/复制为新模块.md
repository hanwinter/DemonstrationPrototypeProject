请在当前项目中新增一个独立模块：“五健健康管理家长端 H5”。

特别注意：
1. 不要修改当前已经定版的“家长端 H5 演示”模块。
2. 不要修改现有“五健智行专案家长端 H5”模块。
3. 本次只做新增模块和路由入口，不调整新模块中的业务内容、页面文案、mock 数据和功能结构。
4. 新模块第一版要完整复制“家长端 H5 演示”模块的现有页面、路由结构、样式、交互和 mock 数据。
5. 复制后新模块必须与原“家长端 H5 演示”模块相互独立，后续修改新模块不能影响原模块。
6. 原有两个模块的页面、路由、样式、mock 数据、交互逻辑都要保持不变。

新增模块名称：
五健健康管理家长端 H5

推荐新增路由：
优先使用：
- /parent/health

如果当前项目已有统一路由命名规范，请按现有规范新增同级入口，例如：
- /parent/demo              家长端 H5 演示
- /parent/zhixing           五健智行专案家长端 H5
- /parent/health            五健健康管理家长端 H5

执行要求：
1. 找到当前“家长端 H5 演示”模块对应的页面目录、路由配置、样式文件、mock 数据和资源引用。
2. 在不修改原模块的前提下，复制一份作为“五健健康管理家长端 H5”模块。
3. 新模块可以先保留原家长端 H5 演示中的全部内容：
   - 首页
   - 问卷
   - 报告
   - 我的
   - 体检报名
   - 复筛通知
   - 就诊人管理
   - 宣教内容
   - 现有 mock 数据
4. 本次不要把内容改成健康管理语境。
5. 本次不要迁入“五健智行专案家长端 H5”的任何功能。
6. 本次不要修改底部 Tab、快捷入口、页面标题、业务文案。
7. 只需要保证新模块可以通过独立路由访问，并且显示效果与原“家长端 H5 演示”一致。

目录建议：
如果当前项目结构允许，建议新增同级目录，例如：

src/
  views/
    parent-demo/          原家长端 H5 演示，不要改
    parent-zhixing/       原五健智行专案家长端 H5，不要改
    parent-health/        新增模块，复制 parent-demo

如果当前项目不是这个结构，请按照现有结构新增同级模块目录，不要破坏原目录。

入口要求：
1. 如果项目有演示入口页或模块选择页，请新增一个入口：
   - 五健健康管理家长端 H5
2. 入口跳转到新模块路由。
3. 原“家长端 H5 演示”入口保持不变。
4. 原“五健智行专案家长端 H5”入口保持不变。

验收标准：
1. 原“家长端 H5 演示”模块完全不受影响。
2. 原“五健智行专案家长端 H5”模块完全不受影响。
3. 新增“五健健康管理家长端 H5”独立模块。
4. 新模块可以通过独立路由访问。
5. 新模块第一版显示内容与“家长端 H5 演示”模块一致。
6. 新模块拥有独立目录、独立路由或独立入口，后续可以单独修改。
7. 本次没有调整新模块的业务内容、页面文案、mock 数据和功能结构。