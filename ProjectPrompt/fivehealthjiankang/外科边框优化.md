五健健康管理家长端 H5：只调整手机预览容器外框样式。

要求：
1. 不修改浏览器背景色，保持当前背景不变。
2. 将当前手机外框的圆角改为直角，border-radius 设置为 0。
3. 取消当前外框的发光效果、绿色光晕或高饱和阴影。
4. 使用一条普通边线体现边框，建议 border: 1px solid #d8e6df 或相近低饱和灰绿色。
5. 可以保留轻微投影，但必须是自然阴影，不要发光效果。
6. 不修改手机内部页面样式，包括顶部栏、内容卡片、底部 Tab、按钮圆角和页面背景。
7. 修改后检查页面尺寸、居中位置、滚动区域不发生变化。