请继续优化“五健健康管理家长端 H5”中的“图文报告 / 查看报告”二级页面 UI 样式。

本次重点：
将图文报告页面的整体 UI 样式，统一为“体检报名”页面这种二级页样式，尤其注意顶部返回按钮、页面背景色、内容区域层级。

参考页面：
以当前项目中的“体检报名”页面为样式参考。

参考重点：
1. 顶部返回按钮样式
2. 页面标题样式
3. 页面背景色
4. 内容卡片背景
5. 页面左右边距
6. 卡片圆角与边界
7. 二级页整体视觉层级

一、顶部返回按钮统一
当前图文报告页面的返回按钮样式不要使用首页那种圆形按钮样式。

请改成与“体检报名”页面一致的顶部样式：
1. 左上角使用简单返回箭头。
2. 返回箭头不放在大圆形浅绿色按钮中。
3. 返回箭头位置、尺寸、颜色与体检报名页一致。
4. 标题紧跟返回箭头右侧，样式与体检报名页一致。
5. 标题建议为：
   - 图文报告
   或
   - 查看报告
6. 顶部区域不要使用首页那种居中标题 + 两侧圆形按钮布局。

目标效果类似：
< 图文报告

而不是：
圆形返回按钮 + 居中标题 + 右侧圆形图标

二、页面背景色统一
图文报告页面背景色要与体检报名页面一致。

要求：
1. 使用体检报名页面的浅绿色页面背景。
2. 不要使用当前首页/主 Tab 页面那种手机外框式背景。
3. 页面内容区域要自然铺开，与体检报名页一致。
4. 保持二级页轻量、清爽的背景层级。

三、内容卡片样式统一
图文报告页面中的报告信息区、工具栏、报告图片查看区，需要与体检报名页面卡片风格一致。

要求：
1. 卡片使用白色背景。
2. 卡片圆角与体检报名页面一致。
3. 卡片边距与体检报名页面一致。
4. 不使用过重边框。
5. 不要使用过大的外层容器圆角。
6. 内容区不要像首页模块卡片，而要像二级页内容卡片。

四、页面结构调整
图文报告页面建议结构：

1. 顶部二级页标题栏
   - 返回箭头
   - 标题：图文报告

2. 报告信息卡片
   - 报告名称：维生素D检测
   - 报告类型：LIS 化验单
   - 检查日期：2026-04-08

3. 工具栏
   - 缩小
   - 放大
   - 适应屏幕

4. 报告图片查看区
   - 展示报告图片
   - 支持缩放
   - 支持滚动/拖动查看

五、保留已有功能
本次只调整 UI 样式，不要破坏之前已经实现的功能。

必须保留：
1. 缩小按钮
2. 放大按钮
3. 适应屏幕按钮
4. 图片缩放能力
5. 图片滚动/拖动查看能力
6. LIS/PACS 图文报告复用同一个查看器
7. 返回上一页能力

六、不要影响其他模块
特别注意：
1. 不要修改当前已经定版的“家长端 H5 演示”模块。
2. 不要修改“五健智行专案家长端 H5”模块。
3. 只修改“五健健康管理家长端 H5”中的图文报告二级页。
4. 不要影响健康管理模块中的首页、专案、报告、我的等其他页面。

七、验收标准
1. 图文报告页顶部返回按钮样式与体检报名页一致。
2. 图文报告页不再使用首页那种圆形返回按钮。
3. 图文报告页顶部标题样式与体检报名页一致。
4. 图文报告页背景色与体检报名页一致。
5. 图文报告页内容卡片风格与体检报名页一致。
6. 缩小、放大、适应屏幕按钮仍然存在。
7. 图文报告图片仍然支持缩放和滚动/拖动查看。
8. 原家长端 H5 演示模块不受影响。
9. 五健智行专案家长端 H5 模块不受影响。