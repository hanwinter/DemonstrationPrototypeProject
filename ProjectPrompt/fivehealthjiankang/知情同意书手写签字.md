在“五健健康管理家长端H5”模块中，优化“专案知情同意书”页面，新增手写签字功能。要求只修改知情同意书相关页面和必要的状态逻辑，不影响其他页面功能，页面整体风格必须与当前“五健健康管理家长端H5”保持统一。

具体要求：

1. 在知情同意书页面增加“家长手写签名”区域
- 放在“我已阅读并理解以上内容”勾选项下方。
- 签名区域采用页面现有卡片/表单风格，不要新增突兀样式。
- 签名框使用白色背景、浅灰边框、小圆角，整体视觉保持轻量。
- 签名框高度控制在 120-150px 左右。
- 未签名前显示提示文字：“请在此处手写签名”。
- 支持移动端触摸签名，PC端鼠标签名。

2. 增加签名操作按钮
- 签名框下方增加两个轻量按钮：“清除重签”“确认签名”。
- 按钮样式沿用当前页面已有按钮规范，不要重新设计一套按钮风格。
- “清除重签”用于清空签名画布。
- “确认签名”后，将当前签名保存为图片或可展示的数据状态。

3. 调整“确认签署”按钮启用规则
“确认签署”按钮需要同时满足以下条件才可点击：
- 已勾选“我已阅读并理解以上内容”
- 已完成手写签名
- 签名内容不能过短，避免只点一下也能确认

如果未完成签名就点击签署，提示：
“请先完成家长手写签名”

如果签名内容过少，提示：
“签名内容过少，请重新签写”

4. 签署成功后的展示
点击“确认签署”后，页面状态改为“已签署”，并在正文下方展示“签署信息”区域，内容包括：
- 签署人：林一凡家长
- 签署时间：使用当前模拟时间，例如 2026-07-23 14:30
- 签名图片：展示刚刚手写签名后的结果

签署信息区域要采用当前页面统一的静态信息展示风格，避免使用过重卡片。签名图片可以放在浅色背景区域内展示，保持清晰可见。

5. 已签署后的页面状态
签署完成后，页面进入只读状态：
- 知情同意书正文仍可查看
- 勾选框不可修改
- 签名图片只读展示
- 不再显示可编辑签名画布
- “确认签署”按钮改为“已完成签署”，并置为不可点击状态
- 不增加“重新签署”“撤回签署”等额外业务功能

6. 页面风格统一要求
- 顶部返回栏、背景色、卡片圆角、字体大小、字段间距、按钮样式，都必须沿用“五健健康管理家长端H5”现有风格。
- 不要引入新的强视觉样式，不要使用大面积深色、渐变、重阴影。
- 页面整体仍保持移动端H5的轻量、清爽、医疗健康风格。
- 与儿童建档、首诊问卷、知情同意书三个顶层入口的二级页视觉保持一致。
- 不要使用弹窗签名，签名区域直接放在当前页面内。

7. 交互演示要求
- 未签署状态：显示正文、勾选项、手写签名区域、确认签署按钮。
- 用户手写签名后，可以点击“确认签名”。
- 点击“确认签署”后，页面立即切换为已签署状态。
- 已签署状态下，可以看到签署信息和签名图片，方便演示“签字后可查看签字内容”的完整闭环。

请完成前端实现，并检查移动端页面是否存在内容拥挤、按钮遮挡、签名框溢出、底部留白异常等问题。