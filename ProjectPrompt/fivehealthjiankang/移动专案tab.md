请继续调整“五健健康管理家长端 H5”模块。

特别注意：
1. 不要修改当前已经定版的“家长端 H5 演示”模块。
2. 不要修改现有“五健智行专案家长端 H5”模块。
3. 本次只调整“五健健康管理家长端 H5”这个新模块。
4. 原有两个模块的页面、路由、样式、mock 数据、交互逻辑都要保持不变。
5. 当前“五健健康管理家长端 H5”是从“家长端 H5 演示”复制出来的新模块，本次在这个新模块内做调整。

调整目标：
在“五健健康管理家长端 H5”中，将底部 Tab 的“问卷”改为“专案”，并把“五健智行专案家长端 H5”中的“专案”Tab 页能力复制到新模块中使用。

同时：
首页快捷入口中的“问卷量表”入口必须保留，问卷功能也必须保留。

一、底部 Tab 调整
当前“五健健康管理家长端 H5”底部 Tab 是：
- 首页
- 问卷
- 报告
- 我的

请调整为：
- 首页
- 专案
- 报告
- 我的

要求：
1. 只修改“五健健康管理家长端 H5”的底部 Tab。
2. 原“家长端 H5 演示”的底部 Tab 不变。
3. 原“五健智行专案家长端 H5”的底部 Tab 不变。
4. “专案”Tab 点击后进入健康管理模块内的新专案页面。
5. 底部 Tab 图标可复用现有合适图标，也可以使用类似“档案/任务/项目”的图标。
6. 底部 Tab 的视觉样式保持当前家长端 H5 风格，间距、选中态、颜色保持一致。

二、专案页面能力复制
请将“五健智行专案家长端 H5”中的“专案”Tab 页内容，复制到“五健健康管理家长端 H5”模块中，作为新模块的“专案”页面。

复制范围包括但不限于：
1. 多儿童/多专案 mock 数据
2. 专案切换
3. 专案生命周期阶段轴
4. 流程 Tab
5. 报告 Tab
6. 家庭服务 Tab
7. 知情同意按专案维度控制
8. 未签署专案的报告/家庭服务锁定态
9. LIS/PACS 报告归档
10. 图文报告查看器
11. 图文报告独立页面或全屏预览层
12. 专案相关样式和交互

注意：
1. 不要移动或删除“五健智行专案家长端 H5”的原文件。
2. 不要让新模块的专案页直接依赖会被后续共同修改的原专案端页面。
3. 如果直接引用原专案端组件会导致后续改动互相影响，请复制一份到“五健健康管理家长端 H5”模块内。
4. 后续“五健健康管理家长端 H5”的专案页可能会单独调整，因此本次要保证它具备独立修改能力。
5. 可以复用通用组件，但不要污染原模块。

三、问卷功能保留
虽然底部 Tab 中的“问卷”改为“专案”，但问卷功能不能删除。

要求：
1. 首页快捷入口中的“问卷量表”入口必须保留。
2. 点击首页快捷入口“问卷量表”后，仍然进入原问卷页面。
3. 原问卷页面、问卷列表、状态、mock 数据、交互逻辑都保留。
4. 问卷页面作为“五健健康管理家长端 H5”的二级页面存在。
5. 不要因为底部 Tab 改为“专案”而删除问卷路由。
6. 从问卷页面返回时，可以返回首页或上一页。
7. 问卷页面不要错误高亮底部“专案”Tab。
8. 如果当前项目二级页面不显示底部 Tab，请按现有规则处理。
9. 如果当前项目二级页面显示底部 Tab，建议问卷页面保持首页选中或不显示选中态，不要选中“专案”。

四、路由建议
如果当前“五健健康管理家长端 H5”的路由类似：

- /parent/health/home
- /parent/health/questionnaire
- /parent/health/report
- /parent/health/profile

请调整为：

底部 Tab 路由：
- /parent/health/home          首页
- /parent/health/project       专案
- /parent/health/report        报告
- /parent/health/profile       我的

保留问卷二级路由：
- /parent/health/questionnaire 问卷量表，从首页快捷入口进入

如果当前项目路由不是上述结构，请按现有项目路由规范实现，但必须满足：
1. 专案是底部一级 Tab。
2. 问卷是首页快捷入口进入的二级页面。
3. 原模块路由不受影响。

五、首页快捷入口
“五健健康管理家长端 H5”首页快捷入口目前包含“问卷量表”。

要求：
1. 保留首页快捷入口中的“问卷量表”。
2. 保留问卷入口图标、文案和跳转能力。
3. 不要把首页快捷入口“问卷量表”改成“专案”。
4. 专案只出现在底部 Tab 中。
5. 首页其他快捷入口暂时不要调整。

六、命名与页面标题
1. 底部 Tab 文案使用“专案”。
2. 新模块中的专案页面标题可以先沿用复制来的内容。
3. 如果需要轻微适配，建议使用：
   - 健康管理专案
   或
   - 五健健康专案
4. 但本次重点是功能迁入，不要大范围改业务文案。

七、样式要求
1. 专案页样式复制“五健智行专案家长端 H5”的专案页当前样式。
2. 保持移动端布局正常。
3. 保持底部 Tab 固定。
4. 保持原家长端 H5 的整体视觉风格。
5. 图文报告查看器打开时，底部 Tab 不应占用报告阅读空间。
6. 不要因为复制专案页导致样式污染其他页面。

八、数据要求
1. 专案页可以先使用“五健智行专案家长端 H5”中的 mock 数据。
2. 问卷页继续使用原“五健健康管理家长端 H5”复制来的问卷 mock 数据。
3. 两套数据不要混乱：
   - 专案页使用专案 mock
   - 问卷页使用问卷 mock
4. 不要求接入真实后端。

九、验收标准
1. 原“家长端 H5 演示”模块完全不受影响。
2. 原“五健智行专案家长端 H5”模块完全不受影响。
3. “五健健康管理家长端 H5”底部 Tab 从“问卷”变为“专案”。
4. 点击“专案”Tab 能进入复制后的专案页面。
5. 专案页面具备“五健智行专案家长端 H5”专案 Tab 的核心能力：
   - 专案切换
   - 生命周期阶段轴
   - 流程/报告/家庭服务 Tab
   - 知情同意控制
   - LIS/PACS 归档
   - 图文报告查看器
6. 首页快捷入口“问卷量表”仍然存在。
7. 点击“问卷量表”仍能进入问卷页面。
8. 问卷功能没有被删除。
9. 问卷页面不会错误高亮底部“专案”Tab。
10. 新模块专案页后续可以独立调整，不会影响“五健智行专案家长端 H5”原专案页。