修复母婴陪护服务端主题样式应用问题。

当前暖杏色主题调整后存在：
- 页面整体灰白，层次不足；
- Radio/Checkbox 选中状态丢失；
- 按钮主次关系不清晰；
- 输入框出现黑色边框；
- 底部操作栏样式异常。

请只修复主题样式应用，不修改页面布局、字段和业务逻辑。


调整要求：

1. 页面层级调整

页面背景：
#F8F6F3（暖白色）

卡片背景：
#FFFFFF

输入区域：
#FFFFFF

让页面形成：
背景 → 白色卡片 → 输入框
三层视觉层级。

避免整体灰色。


2. Button组件修复

提交按钮：

- 背景：#D79A5B
- 文字：白色
- 保持主要操作视觉权重


保存按钮：

- 白色背景
- #D79A5B 边框
- #D79A5B 文字


禁用按钮：

- 不要完全透明；
- 使用浅杏色背景保持可识别。


3. Radio / Checkbox组件修复

当前选中状态颜色丢失。


要求：

选中：

- 边框颜色：#D79A5B
- 内圆点：#D79A5B


未选中：

- 边框：#D8D0C6
- 白色背景


禁止：
- 黑色边框；
- 浏览器默认样式。


4. 输入框样式修复

当前输入框出现黑色边框。


调整：

默认：

border:
#E8E1D8


聚焦：

border:
#D79A5B


禁止：
- 黑色 outline；
- 黑色边框。


5. 条件展开区域

黄疸、脐带等二级字段：

使用浅暖色背景：

#FCF7F0


要求：

- 不使用绿色背景；
- 不使用明显边框；
- 不使用阴影；
- 保持轻量分组。


6. 底部操作栏修复

当前保存按钮漂浮覆盖内容。


调整：

底部操作栏固定：

内容区域
↓
固定操作栏（保存/提交）
↓
底部导航


要求：

- 不覆盖表单内容；
- 保存、提交按钮完整显示；
- 保持移动端安全间距。


7. 滚动条保持暖杏主题：

滑块：
#D8B08A

轨道：
#F3E7DA


最终效果：

从当前“灰白禁用状态”

调整为：

“温暖、专业、高品质母婴陪护服务端”。

只修改主题样式和组件状态，不修改任何业务功能。