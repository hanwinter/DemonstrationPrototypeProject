继续优化母婴陪护服务端页面 UI。

当前修改没有达到效果，存在以下问题：

- 页面整体灰蒙蒙；
- 卡片和背景没有层次；
- Radio/Checkbox 仍是默认样式，选中状态不明显；
- 输入框边框过重；
- 条件区域颜色使用错误。


本次不要简单修改颜色变量，请重新调整页面视觉层级和组件样式。

要求：

一、重新建立页面三层视觉结构

必须实现：

第一层：页面背景

使用：

#F5F7FA

要求：
- 页面整体背景为浅灰白；
- 不允许使用灰蓝色大面积背景。


第二层：业务卡片

所有业务卡片必须：

background:
#FFFFFF

包括：

- 订单信息
- 宝宝数量
- 宝宝1
- 宝妈


要求：

- 卡片与页面背景形成明显区别；
- 圆角 16px；
- 增加轻微阴影：

box-shadow:
0 2px 12px rgba(0,0,0,0.04)

禁止：
- 卡片使用灰色背景；
- 卡片与页面背景颜色接近。


第三层：输入区域

输入框：

background:
#FFFFFF

border:
1px solid #E5E7EB

禁止：
- 黑色边框；
- 浏览器默认 outline。


------------------------------------------------

二、强制重写 Radio 组件样式

当前 Radio 仍使用浏览器默认样式，请完全覆盖。


不要使用：

<input type="radio"> 默认外观


使用自定义样式。


未选中：

- 外圈颜色：
#D1D5DB
- 白色背景


选中：

- 外圈：
#FF6B9A

- 内圆点：
#FF6B9A


效果：

未选：

○ 男


选中：

◉ 女


要求：
用户一眼可以识别当前选择。


------------------------------------------------

三、强制重写 Checkbox 样式

禁止浏览器默认 checkbox。


未选：

白底
浅灰边框


选中：

背景：
#FF6B9A

勾选：
白色


------------------------------------------------

四、重新定义主题色使用范围


主色：

#FF6B9A


只能用于：

- 提交按钮；
- 选中状态；
- 重要操作；
- 状态标签。


禁止：

- 大面积背景使用粉色；
- 条件区域使用粉色。


------------------------------------------------

五、按钮重新设计


提交按钮：

background:
#FF6B9A

color:
#FFFFFF


保存按钮：

background:
#FFFFFF

border:
1px solid #FF6B9A

color:
#FF6B9A


按钮必须清晰可见。

禁止：
- 白色按钮无边界；
- 透明按钮。


------------------------------------------------

六、条件展开区域重新设计


例如：

- 黄疸值
- 趋势
- 分泌物
- 外观


不要使用粉色背景。


调整为：

background:
#F8FAFC


要求：

- 轻量灰色区域；
- 无边框；
- 不做独立卡片；
- 表示附属填写区域。


------------------------------------------------

七、输入框状态


默认：

border:
#E5E7EB


focus：

border:
#FF6B9A


禁止出现：

- 黑色边框；
- 黑色焦点框。


------------------------------------------------

八、整体视觉参考

请参考现代 App UI：

结构：

浅灰背景
↓
白色卡片
↓
轻量输入区域
↓
少量粉色交互强调


不要做成：

- 医疗系统绿色风格；
- 灰色后台系统；
- 默认 HTML 表单。


------------------------------------------------

九、修改范围

只修改：

- CSS
- 组件样式
- UI结构必要调整


不要修改：

- 字段
- 数据
- 接口
- 业务逻辑


完成后重点检查：

1. 卡片是否明显白色浮起；
2. Radio选中是否明显粉色；
3. Checkbox是否有明确勾选状态；
4. 提交按钮是否突出；
5. 页面是否从灰色表单变成高品质母婴服务 App 风格。