请修改家政服务移动端页面 `src/views/NursingHomeLogMiniapp.vue` 中“到岗当日基础情况记录”的初始展示状态。

页面/模块：
- 家政服务移动端
- 到岗当日基础情况记录

当前问题：
- 进入“到岗当日基础情况记录”详情后，当前服务员张洪霞的记录默认是“已发送锁定”状态。
- 页面显示“申请修改”，字段为只读展示。
- 但当前演示需要进入后就是编辑状态，底部显示“保存 / 发送到公司”。

修改目标：
- 当前服务员张洪霞的到岗当日基础情况记录，初始状态改为可编辑状态。
- 进入详情后应显示可编辑表单。
- 底部应显示两个操作按钮：`保存`、`发送到公司`。
- 点击 `保存` 后状态保持/变为 `草稿`，提示“保存成功，未发送到公司”。
- 点击 `发送到公司` 后继续使用现有发送确认弹窗，确认后再进入 `已发送` 锁定状态。

建议修改点：
- 找到 `firstArchives` 的初始化逻辑。
- 当前类似：
  `createArchive('张洪霞', '已发送', '2026-08-01')`
- 改为：
  `createArchive('张洪霞', '草稿', '2026-08-01')`
- 保留另一条历史服务员记录 `李小红` 的状态，不要删除历史服务员记录。

不修改内容：
- 不要删除“已发送锁定 / 申请修改 / 修改申请 / 留痕”的流程。
- 不要改变提交后正式记录不可直接编辑的业务规则。
- 不要重做页面 UI。
- 不要修改路由、底部 Tab、订单信息、宝宝/宝妈字段结构。
- 不要把订单信息变成可编辑表单。
- 不要取消多服务员历史记录。

验收结果：
1. 从“服务”进入“到岗当日基础情况记录”列表后，张洪霞这条记录状态显示为 `草稿` 或可编辑状态。
2. 点击张洪霞记录进入详情，页面不再显示“已发送锁定”提示。
3. 宝宝信息、宝妈信息展示为输入/Radio/Checkbox 等编辑控件。
4. 页面底部显示 `保存` 和 `发送到公司` 两个按钮。
5. 点击 `保存` 后提示“保存成功，未发送到公司”，页面仍可继续编辑。
6. 点击 `发送到公司` 并确认后，状态变为 `已发送`，页面进入锁定态，并显示申请修改入口。
7. 李小红等历史/其他服务员记录仍保留，不被覆盖。