请基于当前项目 C:\AllFiles\jiahufolder\Project_ycf 继续修正“乙肝感染孕产妇”表单 UI。

本次主要修改：
src/views/MaternalReportForms.vue

不要新增依赖。
不要修改路由。
不要影响“危重孕产妇转会诊申请单”表单。
不要影响“梅毒感染/既往感染孕产妇”表单。
不要改变保存、打印、作废等业务逻辑。
本次只修正乙肝感染孕产妇表单中的以下 3 个问题。

====================
一、修正单选按钮布局
====================

当前乙肝表单中仍有单选按钮布局错位的问题。

涉及区域：
- 六、转诊外院建档产检分娩
- 七、新生儿及出院随访

当前问题：
“是 / 否”单选按钮位置明显偏移，radio 圆点和文字没有成组显示，导致医生无法直观看出哪个按钮对应“是”或“否”。

请修正所有“是 / 否”单选项布局。

要求：
1. 每个字段的标签、是/否 radio 必须在同一行内稳定对齐。
2. radio 圆点和对应文字必须紧挨着显示，例如：
   使用转诊单进行转诊： ○ 是   ○ 否
3. 不允许出现 radio 圆点在左、文字跑到很远位置的情况。
4. 不允许出现“是”和“否”分散到大范围横向空间的情况。
5. 可以为该表单单独新增/调整 CSS class，例如 yes-no-row、compact-radio-group 等。
6. 使用 Element Plus radio 时，请检查 el-radio 的 label/value 和显示文本写法，确保文字跟随 radio。
7. 如果当前使用了错误的插槽或布局导致文字脱离 radio，请改为稳定写法。
8. 这次必须实际修正所有乙肝表单里的“是 / 否”单选按钮，不要只改首次乙肝五项。

建议布局形式：
字段名：  ○ 是   ○ 否

示例字段：
- 使用转诊单进行转诊：是 / 否
- 使用转诊知情同意书：是 / 否
- 发放出院指导：是 / 否
- 是否为高暴露需要医疗机构追访：是 / 否

====================
二、加长表格中的输入框
====================

当前抗病毒治疗表格中的“治疗记录”输入框只占单元格宽度的一小部分，看起来像只占了半列。

请调整所有复查/记录表格里的输入框宽度，使其更充分利用表格单元格。

重点修正：
- 五、抗病毒治疗 表格中的“治疗记录”输入框

同时检查：
- 三、HBV DNA（每两月复查）表格输入框
- 四、肝功能检测（每两月复查）表格输入框

要求：
1. 表格内 el-input 宽度尽量占满所在单元格。
2. 建议设置为 width: 100%。
3. “治疗记录”列的输入框应明显变长，接近整列宽度。
4. 表格仍不能横向溢出。
5. 不要通过增加页面整体宽度来解决，要在单元格内部解决。
6. 输入框高度保持当前风格，不要变成大 textarea。
7. 抗病毒治疗表格可以让“治疗记录”列占主要宽度，例如 90% 左右。

====================
三、删除首次乙肝五项下方来源文本
====================

请删除“二、首次乙肝五项”区域下方的这段文本展示：

孕周：35+2周
检查日期：2026-08-05
数据来源：检验报告

要求：
1. 只删除页面展示，不一定删除数据字段。
2. 首次乙肝五项表格仍保留。
3. 五项结果仍为只读 “+ / -” 展示。
4. 不要重新加入“阳性 / 阴性 / 未查”。
5. 不要让这段文本在查看、修改、新建任何模式下出现。

====================
四、限制范围
====================

本次不要做以下事项：

1. 不要改动乙肝表单的分区顺序。
2. 不要新增乙肝字段。
3. 不要删除 HBV DNA、肝功能检测、抗病毒治疗、转诊、出院随访等分区。
4. 不要修改业务状态逻辑。
5. 不要恢复 icon。
6. 不要修改列表页。
7. 不要修改其他系统模块。

====================
五、验收标准
====================

完成后请检查：

1. 乙肝表单所有“是 / 否”单选按钮排列正常。
2. radio 圆点和“是 / 否”文字紧挨着显示。
3. 不再出现 radio 圆点和文字大幅错位。
4. 抗病毒治疗表格中的“治疗记录”输入框明显加长，基本占满单元格。
5. HBV DNA、肝功能检测表格中的输入框也尽量占满单元格。
6. 首次乙肝五项下方不再显示：
   孕周、检查日期、数据来源。
7. 首次乙肝五项仍为只读 “+ / -” 展示。
8. 页面不出现横向滚动条。
9. 其他单据表单不受影响。
10. npm run build 通过。

如果构建失败，请修复到构建通过。

====================
六、交付要求
====================

1. 完成代码修改后，运行 npm run build，确保构建通过。
2. 修改完成后，请更新 ProjectRecord/项目开发记录.md，记录本次修改时间、修改/新增的功能、涉及的主要文件、构建验证结果。
3. 不要删除历史记录，在文件末尾追加本次记录即可。