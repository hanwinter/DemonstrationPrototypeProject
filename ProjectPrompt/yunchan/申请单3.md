请基于当前项目 C:\AllFiles\jiahufolder\Project_ycf 执行本次前端 UI 微调。

本次主要修改：
src/views/MaternalReportForms.vue

不要新增依赖。
不要重构业务逻辑。
不要修改现有表单字段。
不要调整路由。
本次只调整“孕产相关单据”列表页的表格列和操作按钮展示。

====================
一、调整目标
====================

当前页面：
/maternal/report-forms

页面名称：
孕产相关单据

本次调整目标：
让列表更简洁、工整，先突出“当前孕妇的单据目录”定位。
后续如有真实业务需求，再增加更多字段。

====================
二、删除表格列
====================

请删除表格中的：
主要诊断/说明

删除后表格列为：
1. 序号
2. 单据类型
3. 单据编号
4. 状态
5. 最近填写时间
6. 打印时间
7. 操作

注意：
1. 顶部当前孕妇信息行中的“高危因素/主要诊断”继续保留。
2. 不要删除 documents/mock 数据中的 diagnosisOrSummary 字段，避免影响后续扩展。
3. 只是本次列表 UI 不展示该列。

====================
三、操作按钮调整
====================

当前操作列中有“更多”，请去掉“更多”。

操作按钮不要使用 icon，只保留文字。

操作列中统一展示以下按钮：
- 查看
- 修改
- 继续填写
- 打印
- 重新打印
- 作废
- 下载PDF

要求：
1. 每行都展示上述按钮。
2. 当前状态允许的操作可点击。
3. 当前状态不允许的操作置灰，不可点击。
4. 不要用“更多”隐藏操作。
5. 操作按钮保持纯文字，不要 icon。
6. 按钮排列要工整，不能出现某个按钮明显不对齐或不居中的问题。

====================
四、状态与可用操作规则
====================

请按以下规则控制按钮可用/置灰：

状态：未完成
可点击：
- 继续填写
- 作废
不可点击，置灰：
- 查看
- 修改
- 打印
- 重新打印
- 下载PDF

状态：待打印
可点击：
- 查看
- 修改
- 打印
- 作废
- 下载PDF
不可点击，置灰：
- 继续填写
- 重新打印

状态：已打印
可点击：
- 查看
- 修改
- 重新打印
- 下载PDF
不可点击，置灰：
- 继续填写
- 打印
- 作废

状态：已作废
可点击：
- 查看
不可点击，置灰：
- 修改
- 继续填写
- 打印
- 重新打印
- 作废
- 下载PDF

====================
五、操作列样式
====================

请加宽操作列，保证按钮展示工整。

建议：
1. 操作列宽度可以设置为 420px 到 520px，根据实际显示效果选择。
2. 按钮可以横向排列，必要时允许换行。
3. 按钮之间保持统一间距。
4. 整体左对齐或居中对齐都可以，但必须整齐。
5. 禁用按钮颜色使用灰色，仍保持可读。
6. “作废”在可点击时使用危险色；置灰时不要显示红色。
7. 表格整体仍保持当前医疗文书风格，白底、细边框、紧凑。

====================
六、限制范围
====================

本次不要做以下事项：
1. 不要新增单据类型。
2. 不要补充梅毒/乙肝完整医学字段。
3. 不要修改新建、保存、打印、作废等业务逻辑。
4. 不要修改顶部导航。
5. 不要恢复 icon。
6. 不要引入“更多”菜单。
7. 不要删除 diagnosisOrSummary 数据字段，只是不展示该列。

====================
七、验收标准
====================

完成后请检查：

1. 表格中不再显示“主要诊断/说明”列。
2. 顶部孕妇信息中的“高危因素/主要诊断”仍保留。
3. 操作列不再出现“更多”。
4. 操作按钮全部以纯文字展示，无 icon。
5. 每行都展示：
   查看、修改、继续填写、打印、重新打印、作废、下载PDF。
6. 不可用操作已置灰且不可点击。
7. 可用操作仍能触发现有功能。
8. 操作按钮排列工整。
9. npm run build 通过。

如果构建失败，请修复到构建通过。

====================
八、交付要求
====================

1. 完成代码修改后，运行 npm run build，确保构建通过。
2. 修改完成后，请更新 ProjectRecord/项目开发记录.md，记录本次修改时间、修改/新增的功能、涉及的主要文件、构建验证结果。
3. 不要删除历史记录，在文件末尾追加本次记录即可。