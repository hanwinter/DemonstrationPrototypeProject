请继续优化当前“产程记录单打印归档页面”，该页面用于医院病历归档打印，不复用屏幕版 ECharts，继续使用 HTML/CSS 表格 + SVG 绘制。请按以下要求修改：

一、A4页面与整体版式

1. 打印页按 A4 横向设计：
   @page { size: A4 landscape; margin: 10mm 12mm; }
2. 当前内容没有铺满 A4 纸，右侧和下方空白过大，请重新调整打印容器尺寸和表格比例。
3. 打印内容应在 A4 横向页面中居中铺开，主体表格宽度接近可打印区域宽度。
4. 不要把内容集中在页面左上角。
5. 打印页应更接近医院纸质“产程记录单”的归档样式。

二、修复右侧表格边框

1. 当前右侧“处理及附注 / 签字”区域最右侧缺少一条表格边线，请修复。
2. 所有归档表格使用 border-collapse: collapse。
3. 表格外框和内部线都应完整显示。
4. 打印时黑白效果要清晰。

三、字段名称调整

请将打印页中的字段名按纸质产程记录单调整：

1. “宫口扩张”改为“宫口开大厘米”。
2. “胎先露高低”改为“胎头下降厘米”。
3. 图表左下角当前错误显示为“小肘”的位置，改为“时间”。
4. 图例改为：
   - 宫口开大用○表示
   - 胎头下降用×表示

四、日期行和时间行位置调整

主产程图下方的时间记录表按以下顺序展示：

1. 日期
2. 时间
3. 宫缩间隔
4. 宫缩持续时间

当前日期行和时间行位置如果上下颠倒，请互换。

日期行显示日期，例如：15、15、15、15。
时间行显示小时，例如：13、15、17、19。

跨天时，日期行应能显示不同日期，时间行显示对应小时，例如：
日期：15、15、16、16
时间：23、24/0、1、2

五、宫缩图重构

请将宫缩信息改为单独的宫缩图区域，不要只作为普通文字或简单列表。

宫缩图要求：

1. 放在主产程图和日期/时间表格下方。
2. 横向坐标与主产程图完全对齐，使用同一套 0h~24h 时间刻度。
3. 纵向表示宫缩持续时间：
   - 60秒
   - 40秒
   - 20秒
   - <20秒
4. 每一个小时格子中根据宫缩强度显示：
   - 弱：+
   - 中：++
   - 强：+++
5. 宫缩持续时间数据仍然显示在对应时间列中。
6. 打印时不要依赖颜色区分宫缩强度，优先使用 +、++、+++，保证黑白打印清晰。

六、主产程图比例优化

1. 主产程图应作为页面主体，适当放大。
2. 左侧主图区域宽度建议占主体表格的 65%~70%。
3. 右侧“处理及附注 / 签字”区域占 30%~35%。
4. 主图网格线要清晰，但不要过粗。
5. 宫口曲线红色圆点、胎头下降黑色叉号在打印时也要清晰。
6. 如果黑白打印，仍需通过 ○ 和 × 区分两条曲线。

七、底部字段

页面底部保留并对齐以下字段：

1. 估计胎儿大小：3500g
2. 胎位：LOA
3. 破膜时间：2026-06-15 15:30

这些字段横向排列，使用下划线样式，贴近纸质表单。

八、打印要求

1. 打印预览时不显示任何按钮、滚动条、调试信息。
2. 页面内容在 A4 横向纸张内完整显示。
3. 不要出现明显空白过大、内容偏左、内容被裁切的问题。
4. 表格线、文字、曲线在黑白打印下仍然可读。
5. 保持使用独立打印组件，不能直接打印屏幕版 ECharts。