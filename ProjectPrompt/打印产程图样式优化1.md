下面这段可以直接发给 Codex：

```text
请继续优化当前“产程记录单打印归档页面”。该页面用于医院病历归档打印，保持“独立打印组件 + HTML/CSS 表格 + SVG 绘制”的实现方式，不要复用屏幕展示版 ECharts。

本轮重点不是新增字段，而是重构左侧图表区域的网格对齐关系，确保主产程图、时间/日期行、宫缩图之间使用同一套横向时间网格。

一、核心目标

请将左侧打印区域重构为一套“共享 24 小时时间列”的统一网格系统。

要求：

1. 主产程图横坐标的每一个小时格子，必须与下方“时间行、日期行”的每一个格子严格对应。
2. 宫缩图的横向格子，也必须与主产程图使用同一套时间列。
3. 主产程图、时间/日期行、宫缩图、宫缩图下方时间/日期行，所有竖向分隔线必须上下对齐。
4. 相邻区域之间的边框线不要重复绘制，重叠处共用一根线，避免打印后线条变粗。

二、左侧图表区结构调整

请将左侧区域调整为以下结构：

主产程图
↓
时间行
↓
日期行
↓
宫缩图
↓
时间行
↓
日期行

即：

1. 主产程图下方保留一组“时间、日期”行。
2. 宫缩图下方也增加一组“时间、日期”行。
3. 两组“时间、日期”行内容一致，列宽一致，与主产程图和宫缩图完全对齐。

三、删除中间多余行

当前主产程图下方、宫缩图上方的表格中包含：
- 宫缩间隔
- 持续时间

请删除这两行。

主产程图和宫缩图之间只保留：
- 时间
- 日期

宫缩相关信息由下方“宫缩图”表达，不要在中间表格中重复展示。

四、时间与日期行顺序

请统一调整为：

第一行：时间  
第二行：日期

不要使用“日期在上、时间在下”的顺序。

例如：

时间：13、15、17、19、20  
日期：15、15、15、15、15

跨天时：

时间：23、0、1、2  
日期：15、16、16、16

五、去掉坐标原点文字

当前主产程图坐标系原点附近有“时间”两个字，请去掉。

产程图内部不要再额外显示“时间”文字，因为下方已经有专门的时间行。

六、表格线重叠问题

请重点检查并修复打印页中的线条重叠问题。

要求：

1. 相邻表格区域之间共用边框线，不要分别绘制两条线。
2. 不要出现某些线条明显比其他线条更粗的情况。
3. 主产程图底边与时间行顶边共用一根线。
4. 时间行底边与日期行顶边共用一根线。
5. 日期行底边与宫缩图顶边共用一根线。
6. 宫缩图底边与下方时间行顶边共用一根线。
7. 下方时间行底边与下方日期行顶边共用一根线。
8. 整个左侧图表区外框完整，内部线条粗细统一。

如果当前是多个独立 table 或多个独立 SVG 拼接造成边框重叠，请改为：
- 一个统一 SVG 绘制左侧图表区；或
- 一个总表格/统一容器管理边框；
- 避免多个组件各自绘制相邻边框。

七、宫缩图要求

宫缩图继续保留，但需要与主产程图时间轴严格对齐。

宫缩图要求：

1. 横轴：与主产程图相同的 0h~24h 时间列。
2. 每个小时列宽与主产程图一致。
3. 纵向表示宫缩持续时间层级，例如：
   - 60秒
   - 40秒
   - 20秒
   - <20秒
4. 宫缩强度使用符号表示，保证黑白打印可读：
   - 弱：+
   - 中：++
   - 强：+++
5. 不要依赖颜色区分宫缩强度。
6. 宫缩图下方的时间行、日期行也必须与主产程图完全对齐。

八、打印版式保持

除以上结构调整外，保持现有打印页其他内容不变，包括：

1. 页面标题：产程记录单。
2. 顶部产妇基本信息。
3. 右侧“处理及附注 / 签字”区域。
4. 底部字段：
   - 估计胎儿大小
   - 胎位
   - 破膜时间
5. A4 横向打印布局。
6. 打印时不显示按钮、滚动条、调试信息。

九、验收标准

调整后请确认：

1. 主产程图每一小时格与下方时间、日期格严格对齐。
2. 宫缩图每一小时格与主产程图严格对齐。
3. 两套时间/日期行均为“时间在上、日期在下”。
4. 主产程图和宫缩图之间不再显示“宫缩间隔、持续时间”行。
5. 主产程图原点位置不再显示“时间”两个字。
6. 表格线条无明显重叠、加粗、断线问题。
7. 打印预览中整体版式仍能完整显示在 A4 横向页面内。
```
