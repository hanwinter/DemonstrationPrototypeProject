请在当前五健系统前端演示项目的：

**单页面演示**

模块下新增第二个演示页面：

**患者建档**

菜单结构调整为：

* 单页面演示

  * 体检数据
  * 患者建档

不要修改原正式业务模块中的“院内筛查 > 患者建档”页面。本次新增独立页面、独立路由，作为 UI 风格统一改造的演示页面。

---

# 一、页面定位

页面主要用于维护院内筛查患者基础档案，并支持快速为患者创建新的体检记录。

整体 UI 风格继续严格沿用已经完成的“单页面演示 > 体检数据”页面，不重新设计新的视觉风格。

统一采用：

**传统医疗业务管理系统 PC 风格：浅蓝灰、紧凑、高信息密度、表格优先、弱装饰、弱圆角。**

必须复用前一个演示页面已经整理出的公共样式和组件，包括：

* 紧凑查询区
* 传统工具栏
* Grid 表格
* 分页
* Button
* Input
* Select
* 日期控件
* 状态展示

如果前一个页面已经抽取公共组件，则直接复用，不要重新写一套样式。

---

# 二、页面顶部

面包屑修改为：

**单页面演示 > 患者建档**

去掉原页面较明显的浅蓝色波浪装饰背景。

顶部保持简洁：

* 白色或极浅蓝灰背景
* 高度紧凑
* 字号 12~13px
* 不增加说明文字
* 不使用大号页面标题

---

# 三、查询条件区域

原页面查询条件为：

* 姓名
* 证件号
* 电话
* 建档日期
* 建档方式

保留这些条件，但重新压缩排列。

建议一行完成：

```text
姓名：[______]  证件号：[____________]  电话：[__________]
建档方式：[请选择 ▼]  建档日期：[开始日期 至 结束日期]
[查询] [重置]
```

如果当前浏览器宽度不足，可以自动换成两行，但优先保证 1920px PC 环境下一行展示。

### UI要求

* Label：12px
* Input / Select / DatePicker：高度约28px
* 控件之间间距 8~12px
* 查询区域上下 Padding 8~10px
* 不使用卡片包裹
* 不显示“筛选条件”等标题
* 不使用大面积留白

---

# 四、查询字段文案优化

原来的：

**证件号**

建议保留，因为后续可能兼容身份证以外证件类型。

原来的：

**电话**

调整为：

**联系电话**

原来的：

**建档日期**

调整为：

**建档时间**

因为表格实际数据显示精确到时分秒，例如：

`2026-07-23 14:12:55`

查询区也统一使用“建档时间”。

原来的：

**建档方式**

保留。

---

# 五、工具栏重新整理

当前页面右侧操作：

* 重置
* 查询
* 手动新增
* 微信建档

其中：

**查询、重置**属于查询行为。

**手动新增、微信建档**属于业务操作。

不要继续全部放在查询区域右侧。

改为：

### 查询区域

```text
[查询] [重置]
```

### 下方独立工具栏

```text
[新增] [微信建档]
```

“手动新增”调整为：

**新增**

原因：

“手动”属于实现方式，不需要暴露在主按钮文案中。

点击“新增”即代表工作人员手工创建患者档案。

“微信建档”保留，用于区分患者扫码/微信端自主建档。

---

# 六、按钮风格

按钮继续采用之前体检数据页面统一后的传统医疗系统样式。

要求：

* 高度 26~28px
* 字体约12px
* 圆角 2~3px
* 1px 灰色边框
* 白色或浅灰背景
* 图标约14px
* 查询按钮可以使用浅蓝灰背景稍微强调
* 不使用当前高饱和青绿色按钮
* 不使用大面积实心按钮
* 不使用明显阴影

工具栏效果：

```text
[＋ 新增] [ 微信建档 ]
```

图标保持简单、传统。

---

# 七、表格整体改造

当前表格字段：

* 序号
* 姓名
* 证件号
* 性别
* 出生日期
* 年龄
* 联系电话
* 建档方式
* 建档时间
* 操作

整体字段是合理的，可以保留。

调整为传统 Grid Table 风格：

* 表头背景改为 `#EEEEEE` 或 `#F1F1F1`
* 去掉当前大面积浅青色表头
* 表格边框使用浅灰色细线
* 表头高度约30px
* 数据行高度约28px
* 字体约12px
* 一屏尽可能展示更多记录
* 表格占满剩余页面空间

不要使用大卡片或大留白。

---

# 八、表格字段顺序

建议最终字段顺序：

1. 序号
2. 姓名
3. 证件号
4. 性别
5. 出生日期
6. 年龄
7. 联系电话
8. 建档方式
9. 建档时间
10. 操作

该顺序可以保持。

但要注意各列宽度：

* 序号：较窄
* 姓名：80~100px
* 证件号：180~220px
* 性别：60px
* 出生日期：100~120px
* 年龄：60px
* 联系电话：120~140px
* 建档方式：90~110px
* 建档时间：150~170px
* 操作：根据按钮数量控制宽度

避免每列平均分配宽度。

---

# 九、证件号脱敏

当前页面证件号类似：

`370323********2638`

继续保留脱敏展示。

统一建议采用：

```text
370323********2638
```

不要不同记录采用不同数量的星号。

身份证号应保持：

**前6位 + 8个* + 后4位**

演示数据统一格式。

如果存在非身份证证件，可另外处理，但当前 Mock 数据统一使用居民身份证即可。

---

# 十、联系电话数据优化

当前 Mock 数据中部分手机号码不符合常规格式，例如：

`13648862380`
`17612381248`

可以保留正常中国大陆11位手机号。

统一生成更自然的 Mock 数据，例如：

* 13812345678
* 13987654321
* 18612345678
* 13756781234

不要使用明显连续或重复的号码。

如果需要体现隐私保护，也可以统一脱敏为：

```text
138****5678
```

本演示页面建议使用：

**手机号脱敏展示**

即：

`138****5678`

因为患者档案属于敏感个人信息。

---

# 十一、建档方式展示

当前所有记录均为：

**校内建档**

建议 Mock 数据增加不同来源，以便演示筛选功能。

建档方式调整为：

* 院内建档
* 微信建档
* 批量导入

不要继续使用“校内建档”。

原因：

当前页面属于“患者建档”，更偏院内筛查和患者档案，不建议使用“校内建档”这种学校场景表达。

建议数据比例：

* 大部分：院内建档
* 少量：微信建档
* 少量：批量导入

---

# 十二、建档方式状态样式

当前“校内建档”使用绿色胶囊标签。

需要弱化。

改成：

* 小尺寸文字标签
* 高度约20px
* 浅灰 / 浅蓝灰背景
* 圆角2~3px
* 不使用高饱和绿色

例如：

```text
[院内建档]
[微信建档]
[批量导入]
```

三种方式可以稍微使用不同浅色，但整体低饱和。

不要让状态标签成为页面视觉重点。

---

# 十三、操作列重点改造

当前操作：

* 新建体检
* 编辑
* 删除

业务逻辑保留。

文案建议：

**新建体检**改为：

**新增体检**

统一系统动作语言。

最终：

```text
新增体检  编辑  删除
```

操作列采用传统后台文字操作方式，不要做三个大按钮。

样式：

* 新增体检：蓝色文字
* 编辑：蓝色文字
* 删除：红色文字

字号约12px。

操作之间保持8~12px间距。

不要增加背景块和胶囊边框。

---

# 十四、“新增体检”的交互

点击：

**新增体检**

后，为当前患者创建一条新的体检业务记录。

演示项目中至少实现交互反馈，可以：

* 打开体检新增弹窗；
* 或进入已有体检创建页面；
* 如果当前项目暂时没有对应页面，则打开一个简单业务弹窗。

弹窗标题：

**新增体检**

展示患者基础信息：

```text
姓名：李爱心
性别：男
年龄：6岁
证件号：370323********2638
联系电话：138****5678
```

然后可选择：

* 体检批次
* 体检日期
* 体检类型

演示阶段不用扩展过多业务逻辑，但按钮必须可点击并有合理反馈。

---

# 十五、“新增”患者弹窗

点击工具栏：

**新增**

打开传统医疗系统风格弹窗。

不要使用右侧 Drawer，也不要使用全屏页面。

建议尺寸：

约 `520 × 400px`

弹窗标题：

**新增患者**

字段建议：

```text
*姓名：       [____________]

*证件类型：   [居民身份证 ▼]

*证件号码：   [____________]

*性别：       ○男  ○女

*出生日期：   [____-__-__]

年龄：        [自动计算]

联系电话：    [____________]

建档方式：    [院内建档 ▼]

备注：        [________________]
              [________________]
```

规则：

* 必填项使用字段名前红色 `*`
* Label 右对齐
* 输入框高度约28px
* 下拉框使用传统 Select
* 单选使用标准 Radio
* 不要将每个字段做成独立卡片

弹窗底部右侧：

```text
[保存] [关闭]
```

保存按钮可使用浅蓝灰/绿色小图标，但保持传统系统样式。

---

# 十六、微信建档交互

点击：

**微信建档**

后打开一个传统风格小弹窗。

弹窗标题：

**微信建档**

内容可包含：

```text
请使用微信扫描二维码完成患者信息登记。

二维码

有效时间：30分钟
```

底部：

```text
[刷新二维码] [关闭]
```

演示页面使用 Mock 二维码即可。

不要把微信建档做成大型现代营销页面。

---

# 十七、删除交互

点击“删除”时必须二次确认。

弹窗：

**删除确认**

文案：

```text
确定删除患者“李爱心”的档案信息吗？
```

按钮：

```text
[确定] [取消]
```

如果患者已经存在体检记录，演示时可以增加提示：

```text
该患者已存在体检记录，不允许删除。
```

这样更符合医疗业务数据安全逻辑。

---

# 十八、编辑交互

点击“编辑”：

复用“新增患者”弹窗。

标题调整为：

**编辑患者**

自动回填患者数据。

证件号码等关键身份字段可以继续允许演示修改，但如果项目已有业务规则限制，应遵循现有逻辑。

保存后更新表格 Mock 数据。

---

# 十九、年龄字段规则

年龄不要作为独立人工维护字段。

根据：

**出生日期 + 当前日期**

自动计算。

页面只读展示。

例如：

* 出生日期：2020-07-01
* 年龄：6

不要在新增/编辑弹窗中允许工作人员直接填写年龄。

---

# 二十、Mock 数据调整

保留约20条演示数据。

姓名可以继续使用当前数据，也可以适当整理。

要求：

* 男女比例有差异
* 年龄覆盖约4~13岁
* 出生日期与年龄对应
* 手机号格式正常
* 身份证脱敏格式统一
* 建档方式至少包含3种
* 建档时间有不同日期
* 不要全部完全相同

例如：

```text
李爱心 | 男 | 2020-07-01 | 6  | 院内建档
顾禾雪 | 女 | 2022-02-02 | 4  | 微信建档
顾禾雨 | 女 | 2013-01-01 | 13 | 批量导入
高鑫   | 女 | 2016-04-19 | 10 | 院内建档
张颖晨 | 男 | 2020-01-01 | 6  | 微信建档
```

---

# 二十一、分页区域

继续使用已经在“体检数据”演示页确定的统一分页组件。

建议显示：

左侧：

```text
[20条/页 ▼]  <  第1页  共52页  >  刷新
```

右侧：

```text
显示1到20，共1033条记录
```

不要继续全部集中在右下角。

分页区域：

* 高度约30px
* 字体12px
* 浅灰边框
* 白色背景
* 无大圆角

---

# 二十二、页面高度和滚动

当前页面底部有大量空白，需要改善。

要求：

* 查询区和工具栏固定高度
* 表格区域自动占据剩余可视高度
* 表格内部支持纵向滚动
* 表头固定
* 分页固定在页面底部或表格底部
* 避免20条数据结束后下面出现几百像素纯白空白

如果数据不足填满区域，可以保持表格区域高度，但分页紧贴表格容器底部。

---

# 二十三、整体视觉目标

修改后的页面应该明显更接近传统 HIS / 妇幼 / 体检业务系统，而不是现代 SaaS 管理后台。

重点体现：

* 页面紧凑
* 字号较小
* 控件高度低
* 信息密度高
* 表格是绝对视觉主体
* 查询和操作分层清楚
* 状态标签弱化
* 不使用大面积青绿色
* 不使用大圆角卡片
* 不使用强阴影
* 不增加无业务价值的说明性文字

---

# 二十四、开发边界

1. 本页面新增到“单页面演示”模块。
2. 不修改原“院内筛查 > 患者建档”正式页面。
3. 使用独立路由。
4. 尽量复用前一个“体检数据”演示页面建立的公共 UI 组件。
5. 不重新创建一套重复 CSS。
6. 不修改其他系统页面。
7. 保持现有技术栈和项目结构。
8. 本次同时完成页面文案优化及 Mock 数据优化。
9. 后续“单页面演示”下还会继续增加页面，因此保持菜单、路由和公共组件可持续扩展。
