请继续优化“五健家长端 / 儿童健康管理平台”首页 banner 区域。不要重做项目，不要影响登录页、报告页、复筛页、问卷页、我的页，不要修改业务逻辑。本次只将首页 banner 从当前 CSS 文案图形 banner 改为 3 张本地宣传图轮播。

一、调整目标

当前首页 banner 是代码绘制的青绿色背景、文字和五健图标组合。现在需要改为使用 3 张本地做好的宣传图进行轮播展示。

目标效果：
1. banner 使用本地图片。
2. 3 张图片自动轮播。
3. 图片铺满首页 banner 区域。
4. 保留当前首页结构：banner 下方是当前孩子卡、快捷入口、最近筛查、健康宣教。
5. 不再在 banner 上叠加原来的“儿童青少年五健筛查”等文字和图标，避免和图片内容重复。
6. banner 改为本地图片轮播后，前端不要再额外绘制青绿色主色背景、渐变背景或装饰图形；banner 区域的主视觉颜色由图片本身承担，容器只负责尺寸、圆角、裁切、轮播和兜底背景。

二、图片资源路径

请使用以下 3 张本地图片作为 banner 轮播图：

1. ProjectImage\儿童青少年五健筛查平台移动端宣教banner图设计 (1).png
2. ProjectImage\儿童青少年五健筛查平台移动端宣教banner图设计 (2).png
3. ProjectImage\儿童青少年五健筛查平台移动端宣教banner图设计.png

请根据当前项目实际运行方式处理图片引用：

1. 如果前端可以直接引用 ProjectImage 目录，请直接使用以上路径。
2. 如果项目不能直接访问该目录，请将这 3 张图片复制到项目静态资源目录，例如：
   - public/images/banner/banner-1.png
   - public/images/banner/banner-2.png
   - public/images/banner/banner-3.png

然后在代码中使用：

/images/banner/banner-1.png
/images/banner/banner-2.png
/images/banner/banner-3.png

3. 如果项目使用 Vite / Vue / React 的 src assets 方式，也可以复制到：
   - src/assets/banner/banner-1.png
   - src/assets/banner/banner-2.png
   - src/assets/banner/banner-3.png

然后用 import 引入。

三、图片尺寸说明

推荐图片尺寸为：

750 × 360 px

说明：
1. 这是移动端 375px 宽度下的 2 倍图。
2. 页面中显示高度约 180px。
3. 图片比例约 2.08:1。
4. 当前 3 张图片请尽量按 banner 容器比例展示，避免变形和严重裁切。

四、banner 容器样式

请将首页 banner 区域改为图片轮播容器。

样式要求：

1. banner 视觉上铺满手机内容区域宽度。
2. banner 顶部紧接导航栏下方。
3. banner 高度建议 180px。
4. 如果当前页面宽度不是 375px，可按比例自适应。
5. 容器 overflow hidden。
6. banner 保留底部大圆角，建议：
   border-bottom-left-radius: 24px;
   border-bottom-right-radius: 24px;
7. 不要给 banner 过大的左右边距。
8. 不要让 banner 像普通白色卡片。
9. banner 容器背景只保留兜底色，不要额外叠加主色渐变。
10. 兜底背景可使用：
    #EAF8F5

五、图片显示要求

每张 banner 图片要求：

1. width: 100%;
2. height: 100%;
3. display: block;
4. object-fit: cover;

如果发现图片文字被裁切，可以改为：

object-fit: fill;

但优先使用 cover。如果图片本身比例已经接近 750 × 360，使用 cover 应该不会明显裁切。

六、轮播交互

请实现简单轮播逻辑：

1. 默认展示第一张图片。
2. 每 3000ms 自动切换下一张。
3. 3 张图片循环播放。
4. 页面卸载时清理定时器。
5. 支持点击轮播圆点切换图片。
6. 如果项目方便实现左右滑动，可增加滑动切换；如果不方便，演示版只做自动轮播和圆点切换即可。

七、轮播指示点

请保留或新增 3 个轮播指示点。

样式要求：

1. 放在 banner 左下角或底部居中。
2. 当前选中点使用白色或主色。
3. 未选中点使用半透明白色。
4. 圆点尺寸建议：
   - 当前点：16px × 4px，胶囊形
   - 未选中点：6px × 6px，圆形
5. 不要使用过大的指示点。

如果 banner 图片底部信息较多，指示点可放在左下角安全区域。

八、移除原 banner 叠加内容

请移除或隐藏当前 banner 中由代码绘制的内容，包括：

1. 小标签“儿童健康”
2. 主标题“儿童青少年五健筛查”
3. 副标题“关注视力、体重、脊柱、口腔与心理健康”
4. “查看筛查安排”按钮
5. 右侧五健图标组合
6. 半透明装饰圆形
7. 原青绿色渐变背景

说明：
这些内容已经包含在本地宣传图中或由图片统一承载，不要再重复叠加。

九、当前首页其他区域保持

不要修改以下区域的业务结构：

1. 当前孩子卡
2. 快捷入口
3. 最近筛查卡
4. 健康宣教
5. 底部 Tab

如果 banner 高度变化导致当前孩子卡和快捷入口位置不协调，可以只做必要的间距微调。

十、banner 与当前孩子卡关系

当前孩子卡可以继续轻微覆盖 banner 底边。

要求：

1. 当前孩子卡 z-index 高于 banner。
2. 覆盖距离建议 14px-22px。
3. banner 图片底部不要被覆盖重要文字。
4. 图片设计时重要内容应避开底部被覆盖区域。

十一、不要做的事情

1. 不要使用远程图片。
2. 不要接真实接口。
3. 不要重做首页结构。
4. 不要修改登录页。
5. 不要修改报告页、复筛页、问卷页、我的页。
6. 不要保留原 banner 文案叠加在图片上。
7. 不要保留原 banner 青绿色渐变背景和装饰图形。
8. 不要让图片变形严重。
9. 不要让当前孩子卡遮挡 banner 图片的关键文字。

十二、验收标准

完成后应满足：

1. 首页顶部 banner 显示为 3 张本地宣传图片。
2. banner 自动轮播。
3. 轮播圆点可以显示当前图片状态。
4. 原来代码绘制的 banner 文案、图标和背景不再显示。
5. banner 视觉上铺满顶部内容区域。
6. 当前孩子卡、快捷入口、最近筛查、健康宣教保持原有结构。
7. 页面整体仍保持儿童健康管理平台家长端的移动端风格。